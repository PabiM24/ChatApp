My project is a ChatApp built in Java. It allows users to register and login. It validates the username, 
password, and cellphone number, and shows a welcome message when login is successful. All functions were
tested with JUnit and passed,
